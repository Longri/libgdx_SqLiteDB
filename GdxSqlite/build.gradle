apply plugin: "java-library"

sourceSets {
    main {
        java{
            srcDirs = ["src/"]
        }

        resources {
            srcDirs = ["src/"]
            includes = ["**/*.h", "**/*.c","**/*.template"]
        }
    }
}

